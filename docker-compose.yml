services:
  redis:
    image: redis:alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
  api:
    build: .
    ports:
      - "8848:8848"
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/holocron.db
      - SECRET_KEY=${SECRET_KEY:-holocron_secret_key}
      - ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_MINUTES=30
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - redis
    volumes:
      - ./data:/app/data

  frontend:
    build: frontend
    ports:
      - "80:80"
    depends_on:
      - api
